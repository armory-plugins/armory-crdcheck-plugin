name: Compatibility
on: 
  workflow_dispatch:
    inputs:
      payload:
        description: JSON-encoded string containing service versions to test
        required: true
      
jobs:
  compatibility:
    name: Compatibility
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: set up java
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - uses: danielpeach/plugin-test-runner@main
        with: 
          payload: ${{ github.event.inputs.payload }}
